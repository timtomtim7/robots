commands:
  robot:
    help-menu:
      - "&8» &b&lRobots Commands &8«"
      - ""
      - "&b/robots give {player} [amount]"
      - "    &7Gives the target player a robot with an optional desired amount."
    expecting-player:
      - "&8» &cExpecting the name of an online player!"
    give:
      invalid-amount:
        - "&8» &cInvalid amount."
      given:
        - "&8» &7Given &b[player] &b[amount]x &7robot(s)."
      received:
        - "&8» &7Received &b[amount]x &7robot(s)."